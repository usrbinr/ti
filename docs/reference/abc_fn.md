# Classify a group by proportion of a variable (A,B,C,...)

This returns a table that will segment your data into A,B or C segments
based on custom thresholds

## Usage

``` r
abc_fn(x)
```

## Arguments

- x:

  segment object

## Value

a dbi objection
